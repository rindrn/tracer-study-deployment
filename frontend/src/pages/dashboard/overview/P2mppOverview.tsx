import OverviewPageContent from "@/components/dashboard/pages/OverviewPageContent";

/** P2MPP — full institution-wide view (default summary + all KPIs). */
const P2mppOverviewPage = () => <OverviewPageContent />;
export default P2mppOverviewPage;
